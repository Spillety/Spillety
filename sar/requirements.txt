jinja2==3.1.6
# NOTE: validated against locally installed jinja2 3.1.6 (python3 -c "import jinja2").
# Full XSD validation would need lxml/xmlschema, vLLM live inference would need
# the vllm package + model weights; neither is installable in this offline
# environment (no network), so fincen_xsd.py uses stdlib structural checks
# (# ponytail) and the LLM client uses template-fallback.
